# hooks

決定的に強制したい処理を置く。プロンプトの指示は無視されうるが、hookは必ず実行される。

- 例: 編集後のlint・型チェック自動実行、破壊的コマンドのブロック、commit前の禁止パターン検査
- 成功時は沈黙し、失敗時だけエラー文をエージェントへ返す(success is silent, failures are verbose)
- スクリプトは実行権限を付け、`bash -n` が通ること
