# AGENTS.md

AIコーディングエージェント共通の作業規約。毎ターン読み込まれるため短く保つ(目安60行以内)。

運用ルール(ラチェット):
- ここに行を足すのは、実際にエージェントの失敗を観測したときか、外部制約があるときだけ。
- 各行は根拠となる失敗・制約をcommitメッセージで説明できること。
- プロンプトで守らせるより、hooks / harness/guards で機械的に強制できるものはそちらへ移す。
- モデルの能力向上で不要になったルールは削除する。

## 共通規約との関係

ツール非依存の共通規約(応答言語、事前確認の閾値、テストと検証、Git、セキュリティ)は、個人のグローバル設定側の `AGENTS.md` が正である。`~/.claude/CLAUDE.md` から読み込まれ、セッション開始時に自動で入る。本ファイルはそれを上書きせず、このリポジトリ固有の規約のみを扱う。

共通のskillとsubagentも `~/.claude/skills/` と `~/.claude/agents/` に配置済みで、このリポジトリからそのまま呼び出せる。`.claude/` 配下へ複製しない(複製すると更新が二重管理になる)。

注意点:

- 共通規約はこのリポジトリの外にあるため、同じグローバル設定を持たない参加者には適用されない。リポジトリ内のドキュメントで守らせたい規約は、このファイルに明示的に書く。
- ドキュメントで `AGENTS.md` と書くときは、本ファイルとグローバル側のどちらを指すかを必ず区別する。

## 基本方針

- (プロジェクト固有の規約を、失敗を観測してから追記する)
